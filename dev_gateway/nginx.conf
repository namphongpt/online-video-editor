upstream Frontend {
    server front-end:3000;
}

upstream Backend {
    server projects-api:5003;
}

server {
    listen 80;
    
    location / {
        proxy_pass http://Frontend/;
    }

    location /api/ {
        proxy_pass http://Backend/;
    }
}
